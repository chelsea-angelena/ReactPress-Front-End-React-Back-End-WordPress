WordPress theme built with React. Content being served to front end react app through API calls with axios.

To get the main files to start a set up use:
npx  create-react-wptheme

(Must have a local wordpress site up and running to connect to)

